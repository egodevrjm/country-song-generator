# Environment Variables

# Server port (default: 3000)
PORT=3000

# Node environment (development/production)
# NODE_ENV=production

# Optional: If you want to set a default API key (not recommended for public repos)
# ANTHROPIC_API_KEY=your-api-key-here
